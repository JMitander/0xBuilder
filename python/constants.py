from typing import Dict

# Error codes
ERROR_MARKET_MONITOR_INIT: int = 239

# Error messages
ERROR_MESSAGES: Dict[int, str] = {
    ERROR_MARKET_MONITOR_INIT: "Failed to initialize Market Monitor"
    # Add more error codes and messages as needed
}